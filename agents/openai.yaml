interface:
  display_name: "Takeaway Skill"
  short_description: "拆参考、拿机制、先审批，再按范围安全交接"
  default_prompt: "Use $takeaway-skill to distill this reference into a public-safe, unreviewed candidate, separate mechanism from surface style, and hand it downstream only after explicit scope-matched reuse approval."
